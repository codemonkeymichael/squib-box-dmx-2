{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Squib Box DMX 2 UF2",
            "type": "shell",
            "command": "${workspaceFolder}\\build.cmd",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"]
        }
    ]
}
